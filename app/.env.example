# Sauvegarde cloud + comptes (optionnel).
# Sans ces variables, le jeu utilise la sauvegarde locale (localStorage).
# Copier en .env.local et renseigner pour activer Supabase.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
