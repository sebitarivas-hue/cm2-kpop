# Déploiement du jeu Luméria (app/) sur GitHub Pages.
#
# INERTE par défaut : ne s'exécute que sur déclenchement manuel.
# Pour l'activer : Réglages du dépôt > Pages > "Build and deployment"
# > Source : "GitHub Actions". Le proto historique reste accessible sous /legacy/.
name: Deploy Luméria

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: app/package-lock.json
      - name: Install
        working-directory: app
        run: npm ci
      - name: Build
        working-directory: app
        run: npm run build
      - name: Conserver le proto historique sous /legacy
        run: |
          mkdir -p app/dist/legacy
          cp index.html sw.js manifest.json icon-*.png app/dist/legacy/ 2>/dev/null || true
      - uses: actions/upload-pages-artifact@v3
        with:
          path: app/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
